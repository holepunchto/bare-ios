{
  "private": true,
  "name": "bare-ios",
  "version": "0.0.0",
  "description": "Example of embedding Bare in an iOS application",
  "workspaces": [
    "vendor/*"
  ],
  "scripts": {
    "test": "standard",
    "install": "bare-dev vendor sync",
    "configure": "bare-dev configure --platform ios",
    "build": "bare-dev build",
    "start": "bare-dev ios run"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/holepunchto/bare-ios.git"
  },
  "author": "Holepunch",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/holepunchto/bare-ios/issues"
  },
  "homepage": "https://github.com/holepunchto/bare-ios#readme",
  "devDependencies": {
    "brittle": "^3.1.0",
    "standard": "^17.0.0"
  }
}
